@Library('smartlogic-common@ta13301-run-black-duck-scan') _
smartlogic([
  settings: [
    blackduck: [scan: [args: "--detect.excluded.detector.types='pip' --detect.tools.excluded=SIGNATURE_SCAN --detect.excluded.directories.defaults.disabled=true"]],
  ]
])
